# Dependências da API (FastAPI, SQLAlchemy, etc.)
-r api/requirements-api.txt

# Dependências do pynfe (biblioteca local de comunicação SEFAZ)
requests>=2.32.4
signxml>=4.1.0
lxml>=5.4.0
pyopenssl>=24.0
